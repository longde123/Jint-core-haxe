package jint.native.number.dtoa;
using StringTools;
import system.*;
import anonymoustypes.*;

class NumberExtensions
{
    public static function UnsignedShift(l:Float, shift:Int):Float
    {
        return (l >> shift);
    }
    public function new()
    {
    }
}
